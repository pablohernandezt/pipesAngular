import { PipePasswordPipe } from './pipe-password.pipe';

describe('PipePasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
